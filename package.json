{
  "name": "book-store",
  "version": "1.0.0",
  "description": "If your collaborators want to work with the Git Flow workflow in the same repository, they should follow these steps:",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "mocha --timeout 10000",
    "test-ide": "mocha tests/ide/test.js --timeout 20000",
    "test-grid": "mocha tests/grid/runGridTests.js --timeout 20000"
  },
  "engines": {
    "node": ">=18.15.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mocha": "^10.2.0",
    "selenium-webdriver": "^4.19.0"
  }
}
